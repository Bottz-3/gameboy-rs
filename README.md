# gameboy-rs
An audioless Gameboy emulator in Rust rendered via Pixels crate with MBC1 ROM support.
Further MBC support will be added.

## How to Run
Place your .rom in the root folder of gameboy-rs and rename it to "game.gb"
Then cd into the gameboy-rs directory and execute ```cargo run```
